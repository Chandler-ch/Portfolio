<figure class="totd-frame">
	<img id="totd-image" src="/siemensdesirohc.png" alt="Train of the day" />
	<figcaption id="totd-caption">Siemens Desiro HC</figcaption>
</figure>

<style>
	.totd-frame {
		box-sizing: border-box;
		width: 100%;
		margin: 0;
		padding: 10px;
		background-color: var(--boxfill);
		border: 1px solid var(--borders);
		border-radius: 10px;
	}

	.totd-frame img {
		display: block;
		width: 100%;
		aspect-ratio: 16 / 9;
		object-fit: cover;
		object-position: center;
		border-radius: 6px;
	}

	.totd-frame figcaption {
		margin-top: 8px;
		color: var(--text-accent);
		font-size: 0.8rem;
		text-align: center;
	}
</style>
